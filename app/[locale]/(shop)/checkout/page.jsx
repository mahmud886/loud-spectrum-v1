import CheckoutPage from '@/components/checkout/CheckoutPage';

const Checkout = async () => {
  // const getFedexInfo = await getFedexInformations();
  // console.log('getFedexInfo', getFedexInfo);

  // const upsInfo = await getUpsInformations();
  // console.log('upsInfo', upsInfo);

  return (
    <>
      <CheckoutPage />
    </>
  );
};

export default Checkout;
